{
  "name": "agent-annotation-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local observer hub for Crit and Plannotator annotation sessions on macOS. Read-only discovery, loopback-only HTTP UI on 127.0.0.1:7632.",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "parity": "bun run scripts/parity-check.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
